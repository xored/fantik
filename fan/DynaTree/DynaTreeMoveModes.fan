@Js
public enum class DynaTreeMoveModes
{
  ** append a node as last child of a target node.
  child,
  
  ** add a node as sibling before a target node.
  before,
  
  ** add a node as sibling after a target node.
  after
}
